#!/bin/zsh

# Use vim installed by brew instead of system vim.
alias vim=/usr/local/bin/vim

# Enter Python 3 virtual environment.
alias py3="source ~/.py3/bin/activate"

# Connect to hashbang server.
alias hashbang="ssh hashbang"

# Waf is often faster with pypy.
alias pwaf="pypy waf"
